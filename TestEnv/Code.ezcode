//include main

bool running new : true
int index new : 0
list grades new
loop 5 {
	print Please input your grade \(Type END to end\)
	var in new => input
	
	if in == END {
		yield break
	}

	grades append : in
	index + 1
}

print grades

int length new => grades length
float added new => addAll : grades
float result new : (added / length)

print Your Grade: 'result'

method addAll : array => @float {
	return 5
}